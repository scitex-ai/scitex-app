# APP_DEVELOPER_GUIDE

This guide has moved to `src/scitex_app/skills/references/backend-sdk.md`.
It is bundled with the pip package and discoverable via:

```bash
scitex-app skills get backend-sdk
```

Or via MCP: `skills_get(name="backend-sdk")`
